"""gnode — node-based, glitch-first image editor.

This package is the headless evaluation engine and node catalog. The core
(``gnode.core`` + ``gnode.nodes`` + ``gnode.lib``) is pure Python — no web,
no React, no direct disk/network — per the architecture in ``docs/plan.md``.
"""

from __future__ import annotations

__version__ = "0.1.0"
