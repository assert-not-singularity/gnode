"""gnode FastAPI service (Milestone 2).

A conventional localhost SPA backend over the pure engine: it serves the node
catalog, validates and evaluates graphs, and manages image/graph workspaces.
The engine core stays transport-agnostic; this package is one driving adapter.
"""

from __future__ import annotations
